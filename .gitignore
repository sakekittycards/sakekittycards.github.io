# Claude Code local settings (per-machine permissions, not for the repo)
.claude/

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini

# Python
__pycache__/
*.pyc

# Playwright MCP scratch artifacts (snapshots/console logs from browser tooling)
.playwright-mcp/

# Graded-card pipeline runtime data — local-only
scripts/graded-uploader/inbox/
scripts/graded-uploader/finished/
scripts/graded-uploader/out/
scripts/graded-uploader/pricing.csv
scripts/graded-uploader/pricing-edit.csv

# Real-ESRGAN binary + models (~50MB) — install with setup-upscaler.ps1
scripts/graded-uploader/upscaler/

# Raw gallery-dl Instagram dump (~38MB) — only the curated assets/wild/*.jpg
# files ship to the site. Re-download with `gallery-dl https://instagram.com/sakekittycards`
# into assets/instagram/ if you need to recurate.
assets/instagram/
